{
  "name": "project-new",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "compile:pug": "pug -w ./template -o ./ -P",
    "compile:sass": "sass --watch assets/scss:assets/css",
    "start": "npm-run-all -p compile:pug compile:sass",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "npm-run-all": "^4.1.5",
    "run-all": "^1.0.1"
  }
}
